*******reverse a string ***
class Reverse
{
    // Complete the function
    // str: input string
    public static String reverseWord(String str)
    {
        // Reverse the string str
        String strn="";
        for (int i=0;i<str.length();i++){
            strn=strn+Str.charAt(str.length-1-i);
        }
        return strn;
    }
    
}